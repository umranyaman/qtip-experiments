---
title: "qsim_explore_variations"
author: "Ben Langmead"
date: "June 27, 2016"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Libraries

```{r}
library(ggplot2)
library(dplyr)
```

## Analysis of try data

```{r}
m <- read.table(sep=',', quote='', header=T, 'models.csv')
mvsim <- m[m$dir == 'various_simulators',]
```

```{r}
ggplot(mvsim %>% filter(sample == 'sample0.05'), aes(x=param, y=rca)) + geom_boxplot(aes(linetype=training, color=mapq, fill=target)) + scale_y_continuous(limits=c(-15, 15))
```

## Analysis of under/overfitting data

```{r}
muo <- read.table(sep=',', quote='', header=T, 'under_over_fitting.csv')
```

```{r}
ggplot(muo, aes(x=param, y=rca)) + geom_boxplot(aes(fill=training))
```

```{r}
ggplot(muo, aes(x=param, y=rce)) + geom_boxplot(aes(fill=training))
```
